# Agent tooling / workspace metadata
.ai
.hunman
.omo
.codegraph
# .pi/ 其余内容（APPEND_SYSTEM.md、extensions/、skills/）纳入版本管理；仅排除运行时状态
.pi/decision-auditor/
.pi/tasks/

# Dependencies
node_modules/

# Go CLI build artifacts (cli/)
cli/bin/

# Build output
dist/
build/

# Local file storage (dev uploads; prod uses a Docker volume on the host)
api/storage/

# Environment
.env
.env.*

# Logs
*.log
logs/

# OS / editor
.DS_Store
.idea/
*.swp
# 本地隐私工具（含数据路径/DB 凭据，勿提交）
# 一次性导入/回填/诊断脚本都放这里，整个目录忽略
scripts/local/
