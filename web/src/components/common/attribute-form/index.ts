/** Dynamic attribute form engine — shared by users (create dialog, profile tab) and the list filter bar. */

export { AttributeFields } from './fields';
export { buildProfileSchema, buildValueSchema } from './schema';
